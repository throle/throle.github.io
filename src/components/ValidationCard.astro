---
const { validation, limitations } = Astro.props;
---
<div class="validation-card">
  <div>
    <h3>我怎么检查结果</h3>
    <ul>{validation.map((item: string) => <li>{item}</li>)}</ul>
  </div>
  <div>
    <h3>目前的限制</h3>
    <ul>{limitations.map((item: string) => <li>{item}</li>)}</ul>
  </div>
</div>
